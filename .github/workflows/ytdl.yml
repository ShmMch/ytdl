# .github/workflows/youtube-download.yml

name: YouTube Download

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'YouTube Video URL'
        required: true
        type: string
      browser:
        description: 'Browser for cookies'
        required: false
        default: 'chrome'
        type: choice
        options:
          - chrome
          - firefox
          - opera
          - edge
      audio_only:
        description: 'Download only audio'
        required: false
        default: false
        type: boolean

jobs:
  download:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y wget unzip xvfb ffmpeg
        wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
        sudo apt install ./google-chrome-stable_current_amd64.deb
        python -m pip install --upgrade pip
        pip install yt-dlp browser_cookie3 pathlib

    - name: Setup virtual display
      run: |
        Xvfb :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &
        echo "export DISPLAY=:99" >> $GITHUB_ENV

    - name: Create downloader script
      env:
        SCRIPT: |
          import yt_dlp
          from pathlib import Path
          import logging
          import browser_cookie3
          import tempfile
          import os
          
          class VideoDownloader:
              def __init__(self, output_dir="downloads"):
                  self.output_dir = Path(output_dir)
                  self.output_dir.mkdir(parents=True, exist_ok=True)
                  logging.basicConfig(level=logging.INFO)
                  self.logger = logging.getLogger(__name__)
          
              def _get_browser_cookies(self, browser='chrome'):
                  try:
                      cookie_extractors = {
                          'chrome': browser_cookie3.chrome,
                          'firefox': browser_cookie3.firefox,
                          'opera': browser_cookie3.opera,
                          'edge': browser_cookie3.edge
                      }
                      
                      if browser not in cookie_extractors:
                          self.logger.error(f"Unsupported browser: {browser}")
                          return None
                      
                      cookie_file = tempfile.NamedTemporaryFile(delete=False, suffix='.txt')
                      cookie_path = cookie_file.name
                      
                      cj = cookie_extractors[browser](domain_name='youtube.com')
                      with open(cookie_path, 'w', encoding='utf-8') as f:
                          for cookie in cj:
                              f.write(f"{cookie.domain}\tTRUE\t{cookie.path}\t"
                                    f"{'TRUE' if cookie.secure else 'FALSE'}\t{cookie.expires}\t"
                                    f"{cookie.name}\t{cookie.value}\n")
                      return cookie_path
                  except Exception as e:
                      self.logger.error(f"Error extracting cookies: {str(e)}")
                      return None
          
              def download_video(self, url, browser='chrome', audio=False):
                  cookie_file = None
                  try:
                      cookie_file = self._get_browser_cookies(browser)
                      
                      ydl_opts = {
                          'format': 'bestaudio[ext=m4a]/best' if audio else 'bestvideo[ext=mp4]+bestaudio[ext=m4a]/mp4',
                          'outtmpl': str(self.output_dir / '%(title)s.%(ext)s'),
                          'postprocessors': [{
                              'key': 'FFmpegExtractAudio',
                              'preferredcodec': 'mp3',
                              'preferredquality': '192',
                          }] if audio else [],
                          'verbose': True,
                          'progress_hooks': [self._progress_hook]
                      }
                      
                      if cookie_file:
                          ydl_opts['cookiefile'] = cookie_file
                      
                      with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                          self.logger.info(f"Starting download of {url}")
                          info = ydl.extract_info(url, download=True)
                          return True
                          
                  except Exception as e:
                      self.logger.error(f"Error: {str(e)}")
                      return False
                  finally:
                      if cookie_file and os.path.exists(cookie_file):
                          try:
                              os.unlink(cookie_file)
                          except:
                              pass
          
              def _progress_hook(self, d):
                  if d['status'] == 'downloading':
                      try:
                          if 'total_bytes' in d:
                              downloaded = d.get('downloaded_bytes', 0)
                              total = d['total_bytes']
                              percentage = (downloaded / total) * 100
                              self.logger.info(f"Progress: {percentage:.1f}%")
                          elif 'downloaded_bytes' in d:
                              mb = d['downloaded_bytes'] / 1024 / 1024
                              self.logger.info(f"Downloaded: {mb:.1f} MB")
                      except:
                          pass
                  elif d['status'] == 'finished':
                      self.logger.info(f"Finished downloading")
      run: echo "$SCRIPT" > youtube_downloader.py

    - name: Create runner script
      env:
        SCRIPT: |
          import sys
          from youtube_downloader import VideoDownloader
          
          def main():
              url = sys.argv[1]
              browser = 'chrome'
              audio = False
              
              args = sys.argv[2:]
              i = 0
              while i < len(args):
                  if args[i] == '--browser':
                      browser = args[i + 1]
                      i += 2
                  elif args[i] == '--audio':
                      audio = True
                      i += 1
                  else:
                      i += 1
              
              downloader = VideoDownloader()
              if not downloader.download_video(url, browser=browser, audio=audio):
                  sys.exit(1)
          
          if __name__ == '__main__':
              main()
      run: echo "$SCRIPT" > run_downloader.py

    - name: Run downloader
      env:
        DISPLAY: :99
      run: |
        python run_downloader.py "${{ github.event.inputs.video_url }}" \
          --browser "${{ github.event.inputs.browser }}" \
          ${{ github.event.inputs.audio_only == 'true' && '--audio' || '' }}

    - name: Upload downloads
      uses: actions/upload-artifact@v4.3.1
      with:
        name: downloads
        path: downloads/*
        retention-days: 5
        compression-level: 0
        overwrite: true
