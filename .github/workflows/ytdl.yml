name: YouTube Download Workflow

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'YouTube Video URL'
        required: true
        type: string
      browser:
        description: 'Browser for cookies'
        required: false
        default: 'chrome'
        type: choice
        options:
          - chrome
          - firefox
          - opera
          - edge
      audio_only:
        description: 'Download only audio'
        required: false
        default: false
        type: boolean

jobs:
  download:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install Chrome
      run: |
        sudo apt-get update
        sudo apt-get install -y wget unzip xvfb ffmpeg
        wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
        sudo apt install ./google-chrome-stable_current_amd64.deb
        google-chrome --version

    - name: Install Firefox
      if: ${{ github.event.inputs.browser == 'firefox' }}
      run: |
        sudo apt-get install -y firefox
        firefox --version

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install yt-dlp browser_cookie3 pathlib

    - name: Create downloader script
      run: |
        cat > youtube_downloader.py << 'EOL'
# Your youtube_downloader.py content goes here
EOL

    - name: Create runner script
      run: |
        cat > run_downloader.py << 'EOL'
# Your run_downloader.py content goes here
EOL

    - name: Setup virtual display
      run: |
        Xvfb :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &
        echo "export DISPLAY=:99" >> $GITHUB_ENV

    - name: Run downloader
      env:
        DISPLAY: :99
      run: |
        python run_downloader.py "${{ github.event.inputs.video_url }}" \
        --browser "${{ github.event.inputs.browser }}" \
        ${{ github.event.inputs.audio_only == 'true' && '--audio' || '' }}

    - name: Upload downloads
      uses: actions/upload-artifact@v4.3.1
      with:
        name: downloads
        path: downloads/*
        retention-days: 5
        compression-level: 0
        overwrite: true
